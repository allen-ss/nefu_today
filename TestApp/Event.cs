using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;



namespace App3
{
    class Event
    {
        private String Time { get; set; }
        private String Name { get; set; }
        private String Teacher { get; set; }
        private String Room { get; set; }
        private String Comment { get; set; }
        public Event(string html)
        {
            //string res = match.ToString().Replace("<optgroup label=", "").Replace(">", "").Replace("<option value=", "").Replace("selected", "").Replace("\"", "").Replace("=", "");
        }
    }
}